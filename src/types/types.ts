export type BoardContentsPropsType = {
    boardName: string
}

export type PostType = {
    title: string,
    content: string,
    category: string
}

export type PostItemType = {
    id:number,
    title:string,
    content:string,
    category:string,
}